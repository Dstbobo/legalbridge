# Copy this file to android/keystore.properties (one level up from app/)
# and fill in your real values. NEVER commit keystore.properties to git.
storeFile=release.keystore
storePassword=YOUR_STORE_PASSWORD
keyAlias=legalbridge
keyPassword=YOUR_KEY_PASSWORD
